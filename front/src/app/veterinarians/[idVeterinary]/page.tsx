'use client';

import NotFound from '../../components/WorkInProgress/WorkInProgress';

interface TimeSlot {
  id: string;
  time: string;
  available: boolean;
}

export default function VeterinaryDetailPage() {
 
  return (
    <div>
      <NotFound/>
    </div>
  );
}
